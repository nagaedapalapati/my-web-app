version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 14 # Specify your runtime
  build:
    commands:
      - echo "Building the application..."
      - npm install # or pip install, etc.
      - npm run build # or other build commands
artifacts:
  files:
    - '**/*' # Include all files in the artifact
